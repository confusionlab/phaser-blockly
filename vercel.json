{
  "framework": "vite",
  "buildCommand": "pnpm --filter @pochacoding/web build",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
